const USERNAME_OR_PASSWORD_OR_EMAIL_IS_EMPTY =
  'username, password or email is empty'

const USER_ALREADY_EXISTS = 'user already exists'

module.exports = {
  USERNAME_OR_PASSWORD_OR_EMAIL_IS_EMPTY,
  USER_ALREADY_EXISTS,
}
